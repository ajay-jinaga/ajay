#include<stdio.h>
int main()
{
  int w,h;
  float a;
  printf("key wis the width of rectangle for which area to be found out");
printf("key h is the height of rectangle for which area to be found out");
scanf("%d",&w);
scanf("%d",&h);
a=w*h;
printf("the value of w=%d & value of h=%d\n",w,h);
printf("the area of rectangle a=%.2f\n",a);
}
  
